---
// Landing page for Heartbeat Intelligence
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<meta name="description" content="Trade in intelligence, not just information. Connect with domain experts who curate and monetize exclusive intel." />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<title>Heartbeat | Intelligence Exchange Platform</title>
		<style>
			:root {
				--primary: #FF4500;
				--text: #1a1a1a;
				--text-muted: #666;
				--bg: #fafafa;
			}
			* { box-sizing: border-box; margin: 0; padding: 0; }
			body {
				font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
				background: var(--bg);
				color: var(--text);
				line-height: 1.6;
			}
			.container { max-width: 1200px; margin: 0 auto; padding: 0 24px; }
			
			/* Header */
			header {
				padding: 20px 0;
				border-bottom: 1px solid #eee;
			}
			.logo {
				font-size: 24px;
				font-weight: 700;
				letter-spacing: -0.5px;
			}
			
			/* Hero */
			.hero {
				padding: 100px 0;
				text-align: center;
				background: linear-gradient(180deg, #fff3ed 0%, #fafafa 100%);
			}
			.badge {
				display: inline-block;
				background: var(--primary);
				color: white;
				padding: 8px 16px;
				border-radius: 20px;
				font-size: 12px;
				font-weight: 600;
				text-transform: uppercase;
				letter-spacing: 1px;
				margin-bottom: 24px;
			}
			h1 {
				font-size: 56px;
				font-weight: 700;
				line-height: 1.1;
				margin-bottom: 24px;
				letter-spacing: -2px;
			}
			h1 span { color: var(--primary); }
			.hero p {
				font-size: 20px;
				color: var(--text-muted);
				max-width: 600px;
				margin: 0 auto;
			}
			
			/* Featured Experts */
			.featured {
				padding: 80px 0;
			}
			.section-label {
				color: var(--primary);
				font-size: 12px;
				font-weight: 600;
				text-transform: uppercase;
				letter-spacing: 1px;
				margin-bottom: 8px;
			}
			h2 {
				font-size: 32px;
				font-weight: 600;
				margin-bottom: 40px;
			}
			.experts-grid {
				display: grid;
				grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
				gap: 24px;
			}
			.expert-card {
				background: white;
				border-radius: 12px;
				padding: 24px;
				border: 1px solid #eee;
				transition: box-shadow 0.2s;
			}
			.expert-card:hover {
				box-shadow: 0 10px 40px rgba(0,0,0,0.08);
			}
			.expert-name {
				font-size: 18px;
				font-weight: 600;
				margin-bottom: 4px;
			}
			.expert-role {
				color: var(--text-muted);
				font-size: 14px;
			}
			
			/* CTA */
			.cta {
				background: var(--text);
				color: white;
				padding: 80px 0;
				text-align: center;
			}
			.cta h2 { color: white; margin-bottom: 16px; }
			.cta p { color: #aaa; margin-bottom: 32px; }
			.btn {
				display: inline-block;
				background: var(--primary);
				color: white;
				padding: 16px 32px;
				border-radius: 8px;
				text-decoration: none;
				font-weight: 600;
				transition: background 0.2s;
			}
			.btn:hover { background: #e03e00; }
			
			/* Footer */
			footer {
				padding: 40px 0;
				border-top: 1px solid #eee;
				text-align: center;
				color: var(--text-muted);
				font-size: 14px;
			}
		</style>
	</head>
	<body>
		<header>
			<div class="container">
				<div class="logo">HEARTBEAT</div>
			</div>
		</header>
		
		<section class="hero">
			<div class="container">
				<span class="badge">Intelligence Exchange Platform</span>
				<h1>Trade in <span>intelligence</span>,<br/>not just information</h1>
				<p>Connect with domain experts who curate and monetize exclusive intel. Subscribe to insights that give you an edge.</p>
			</div>
		</section>
		
		<section class="featured">
			<div class="container">
				<div class="section-label">Featured Experts</div>
				<h2>Premium Intelligence Providers</h2>
				<div class="experts-grid">
					<div class="expert-card">
						<div class="expert-name">Diego Alcaíno</div>
						<div class="expert-role">VC & Startup Intelligence</div>
					</div>
					<!-- More experts loaded dynamically or at build time -->
				</div>
			</div>
		</section>
		
		<section class="cta">
			<div class="container">
				<h2>Become an Intelligence Provider</h2>
				<p>Monetize your expertise. Set up your own Heartbeat and start selling curated intel to subscribers worldwide.</p>
				<a href="https://calendly.com/heartbeat-demo" class="btn">Book a Demo →</a>
			</div>
		</section>
		
		<footer>
			<div class="container">
				© 2024 Heartbeat Intelligence. All rights reserved.
			</div>
		</footer>
	</body>
</html>
