name = "heartbeat-router"
main = "router.js"
compatibility_date = "2024-01-01"

# Route configuration — deploy this Worker on heartbeatintel.com
# The route must be configured in the Cloudflare dashboard:
#   Route pattern: *heartbeatintel.com/*
#   Worker: heartbeat-router
# The zone is on a different CF account than the Workers account.
